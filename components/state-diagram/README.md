# 组件 - 状态机图 (State Diagram)

本组件用于定义复杂的系统状态变迁，如 Wi-Fi DFS 避让机制、电视焦点聚焦状态机等。

## 1. 状态变迁模板
请参考 `skills/dfs/SKILL.md` 中雷达避让状态机的 Mermaid 语法。

## 2. 设计规范
* 标明初始状态 `[*]`。
* 每一个变迁箭头上，必须用中文写清触发该变迁的条件/事件（如：检测到雷达信号）。
